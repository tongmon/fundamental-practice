#include <bits/stdc++.h>
using namespace std;

/*
https://www.acmicpc.net/problem/18511
*/

int n, k;
vector<int> a;

int main()
{
	ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
	cin >> n >> k;
	a.resize(k);
	for (int i = 0; i < k; i++)
		cin >> a[i];
}